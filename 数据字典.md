# 📚 图书管理系统 - 数据字典

---

## 一、用户角色定义

| 角色代码 | 角色名称 | 英文标识 | 权限说明 |
|---------|---------|---------|---------|
| `0` | 读者 | reader | 可借阅图书、查看个人借阅记录、接收消息 |
| `1` | 管理员 | admin | 可管理图书、读者用户、查看日志 |
| `2` | 超级管理员 | super_admin | 拥有全部权限，可管理管理员和系统设置 |

---

## 二、数据库表结构

### 1. user（用户表）

| 字段名 | 类型 | 约束 | 说明 |
|-------|------|------|------|
| `userId` | INT | PRIMARY KEY, AUTO_INCREMENT | 用户唯一标识 |
| `userName` | VARCHAR(20) | NOT NULL | 用户名 |
| `userPassword` | VARCHAR(20) | NOT NULL | 用户密码 |
| `isAdmin` | TINYINT | NOT NULL | 角色标识（0=读者, 1=管理员, 2=超级管理员） |
| `balance` | DECIMAL(10,2) | NOT NULL, DEFAULT 0.00 | 账户余额（押金） |

### 2. book_info（图书信息表）

| 字段名 | 类型 | 约束 | 说明 |
|-------|------|------|------|
| `bookId` | INT | PRIMARY KEY, AUTO_INCREMENT | 图书唯一标识 |
| `bookName` | VARCHAR(50) | NOT NULL | 图书名称 |
| `bookAuthor` | VARCHAR(50) | NOT NULL | 作者 |
| `bookPrice` | DECIMAL(10,2) | NOT NULL | 价格 |
| `bookTypeId` | INT | NOT NULL, FOREIGN KEY | 图书类型ID |
| `bookDesc` | VARCHAR(255) | NOT NULL | 书籍描述 |
| `isBorrowed` | TINYINT | NOT NULL | 是否借出（0=可借阅, 1=已借出） |
| `bookImg` | VARCHAR(255) | NULL | 封面图片URL |
| `isOffShelf` | TINYINT | DEFAULT 0 | 是否已下架（0=在架, 1=下架） |

### 3. book_type（图书类型表）

| 字段名 | 类型 | 约束 | 说明 |
|-------|------|------|------|
| `bookTypeId` | INT | PRIMARY KEY, AUTO_INCREMENT | 类型唯一标识 |
| `bookTypeName` | VARCHAR(20) | NOT NULL | 类型名称 |
| `bookTypeDesc` | VARCHAR(255) | NOT NULL | 类型描述 |

### 4. borrow（借阅记录表）

| 字段名 | 类型 | 约束 | 说明 |
|-------|------|------|------|
| `borrowId` | INT | PRIMARY KEY, AUTO_INCREMENT | 借阅记录唯一标识 |
| `userId` | INT | NOT NULL, FOREIGN KEY | 用户ID |
| `bookId` | INT | NOT NULL, FOREIGN KEY | 图书ID |
| `borrowTime` | DATETIME | NOT NULL | 借阅时间 |
| `returnTime` | DATETIME | NULL | 归还时间（NULL表示未归还） |
| `renewCount` | INT | DEFAULT 0 | 续借次数 |
| `dueTime` | DATETIME | NULL | 到期时间 |

### 5. operation_log（操作日志表）

| 字段名 | 类型 | 约束 | 说明 |
|-------|------|------|------|
| `logId` | INT | PRIMARY KEY, AUTO_INCREMENT | 日志唯一标识 |
| `userId` | INT | NOT NULL | 操作人ID |
| `userName` | VARCHAR(20) | NOT NULL | 操作人姓名 |
| `operation` | VARCHAR(100) | NOT NULL | 操作类型 |
| `target` | VARCHAR(100) | NULL | 操作对象 |
| `createTime` | DATETIME | DEFAULT CURRENT_TIMESTAMP | 操作时间 |

### 6. system_config（系统配置表）

| 字段名 | 类型 | 约束 | 说明 |
|-------|------|------|------|
| `configId` | INT | PRIMARY KEY, AUTO_INCREMENT | 配置唯一标识 |
| `configKey` | VARCHAR(50) | NOT NULL, UNIQUE | 配置键名 |
| `configValue` | VARCHAR(255) | NOT NULL | 配置值 |
| `configDesc` | VARCHAR(100) | NULL | 配置描述 |

### 7. transaction（交易流水表）

| 字段名 | 类型 | 约束 | 说明 |
|-------|------|------|------|
| `transactionId` | INT | PRIMARY KEY, AUTO_INCREMENT | 交易唯一标识 |
| `userId` | INT | NOT NULL | 用户ID |
| `type` | VARCHAR(20) | NOT NULL | 类型：recharge/deposit/refund/fine |
| `amount` | DECIMAL(10,2) | NOT NULL | 金额（正=收入，负=支出） |
| `description` | VARCHAR(255) | NULL | 描述 |
| `createTime` | DATETIME | NOT NULL | 创建时间 |

### 8. message（消息表）

| 字段名 | 类型 | 约束 | 说明 |
|-------|------|------|------|
| `messageId` | INT | PRIMARY KEY, AUTO_INCREMENT | 消息唯一标识 |
| `userId` | INT | NOT NULL | 接收用户ID |
| `title` | VARCHAR(100) | NOT NULL | 消息标题 |
| `content` | VARCHAR(500) | NOT NULL | 消息内容 |
| `isRead` | TINYINT | DEFAULT 0 | 是否已读（0=未读, 1=已读） |
| `createTime` | DATETIME | DEFAULT CURRENT_TIMESTAMP | 发送时间 |

---

## 三、系统配置项

### 借阅规则配置

| 配置键 | 说明 | 默认值 | 示例值 |
|-------|------|-------|-------|
| `borrow_days` | 默认借阅天数 | 14 | 22 |
| `max_renew_count` | 最大续借次数 | 1 | 2 |
| `max_borrow_count` | 每人最大借阅数量 | 5 | 3 |
| `renew_days` | 续借延长天数 | 14 | 7 |

### 注册与权限控制

| 配置键 | 说明 | 默认值 | 示例值 |
|-------|------|-------|-------|
| `invite_code` | 管理员注册邀请码 | BMS2024 | ABC12345 |
| `allow_register` | 是否允许开放注册 | false | true |
| `default_role` | 新用户默认角色 | 0 | 0 |

### 提醒通知设置

| 配置键 | 说明 | 默认值 | 示例值 |
|-------|------|-------|-------|
| `remind_days` | 到期前提醒天数 | 3 | 7 |
| `fine_amount` | 逾期罚款金额（元/天） | 1 | 1 |
| `fine_amount` | 逾期罚款金额（元/天） | 0.50 | 1.00 |
| `enable_notification` | 是否启用消息推送 | true | true |

### 系统基本信息

| 配置键 | 说明 | 默认值 | 示例值 |
|-------|------|-------|-------|
| `library_name` | 图书馆名称 | (空) | 智慧图书馆 |
| `contact_phone` | 联系电话 | (空) | 400-123-4567 |
| `opening_hours` | 开放时间说明 | (空) | 周一至周五 8:00-18:00 |
| `system_notice` | 系统公告内容 | (空) | 欢迎使用图书管理系统 |

### 安全策略

| 配置键 | 说明 | 默认值 | 示例值 |
|-------|------|-------|-------|
| `min_password_length` | 密码最小长度 | 8 | 6 |
| `max_login_attempts` | 登录失败锁定次数 | 5 | 3 |
| `force_change_password` | 是否强制修改初始密码 | true | true |

### 数据管理

| 配置键 | 说明 | 默认值 | 示例值 |
|-------|------|-------|-------|
| `log_retention_days` | 日志保留天数 | 90 | 30 |

---

## 四、API接口汇总


### 用户管理接口

| 接口路径 | HTTP方法 | 说明 |
|---------|---------|------|
| `/user/login` | POST | 用户登录 |
| `/user/register` | POST | 用户注册 |
| `/user/logout` | POST | 用户退出 |
| `/user/checkRole` | GET | 根据用户名查询用户身份 |
| `/user/info` | GET | 获取用户信息 |
| `/user/alterPassword` | POST | 修改密码 |
| `/user/getCount` | GET | 获取用户数量 |
| `/user/queryUsers` | GET | 查询所有用户 |
| `/user/queryUsersByPage` | GET | 分页查询用户 |
| `/user/queryUsersByPageWithAuth` | GET | 分页查询用户（带权限过滤） |
| `/user/addUser` | POST | 添加用户 |
| `/user/deleteUser` | DELETE | 删除用户 |
| `/user/deleteUsers` | DELETE | 批量删除用户 |
| `/user/updateUser` | POST | 更新用户信息 |
| `/user/balance` | GET | 获取当前用户余额 |
| `/user/recharge` | POST | 管理员为用户充值 |
| `/user/transactions` | GET | 获取当前用户交易流水 |


### 图书管理接口

| 接口路径 | HTTP方法 | 说明 |
|---------|---------|------|
| `/bookInfo/getCount` | GET | 获取图书数量 |
| `/bookInfo/queryBookInfos` | GET | 查询所有图书信息 |
| `/bookInfo/queryBookInfosByPage` | GET | 分页查询图书信息 |
| `/bookInfo/queryBooksForReader` | GET | 读者端查询图书（排除已下架） |
| `/bookInfo/addBookInfo` | POST | 添加图书信息 |
| `/bookInfo/deleteBookInfo` | DELETE | 删除图书信息 |
| `/bookInfo/deleteBookInfos` | DELETE | 批量删除图书信息 |
| `/bookInfo/updateBookInfo` | PUT | 更新图书信息 |
| `/bookInfo/bookCountByType` | GET | 按类型统计图书数量 |


### 借阅管理接口

| 接口路径 | HTTP方法 | 说明 |
|---------|---------|------|
| `/borrow/getCount` | GET | 获取借阅数量 |
| `/borrow/queryBorrowsByPage` | GET | 分页查询借阅记录 |
| `/borrow/addBorrow` | POST | 添加借阅记录 |
| `/borrow/deleteBorrow` | DELETE | 删除借阅记录 |
| `/borrow/deleteBorrows` | DELETE | 批量删除借阅记录 |
| `/borrow/updateBorrow` | POST | 更新借阅记录 |
| `/borrow/borrowBook` | POST | 借书 |
| `/borrow/returnBook` | POST | 还书 |
| `/borrow/renewBorrow` | GET | 续借 |
| `/borrow/weeklyStats` | GET | 近7日借还统计 |


### 系统管理接口

| 接口路径 | HTTP方法 | 说明 |
|---------|---------|------|
| `/system/logs/queryByPage` | GET | 分页查询操作日志 |
| `/system/logs/add` | POST | 添加操作日志 |
| `/system/config/getAll` | GET | 获取所有系统配置 |
| `/system/config/update` | POST | 更新单个配置项 |
| `/system/config/batchUpdate` | POST | 批量更新配置项 |
### 图书类型接口

| 接口路径 | HTTP方法 | 说明 |
|---------|---------|------|
| `/bookType/getCount` | GET | 获取图书类型数量 |
| `/bookType/queryBookTypes` | GET | 查询所有图书类型 |
| `/bookType/queryBookTypesByPage` | GET | 分页查询图书类型 |
| `/bookType/addBookType` | POST | 添加图书类型 |
| `/bookType/deleteBookType` | DELETE | 删除图书类型 |
| `/bookType/deleteBookTypes` | DELETE | 批量删除图书类型 |
| `/bookType/updateBookType` | PUT | 更新图书类型 |

### 消息中心接口

| 接口路径 | HTTP方法 | 说明 |
|---------|---------|------|
| `/message/sendMessage` | POST | 发送消息 |
| `/message/queryMessagesByPage` | GET | 分页查询消息 |
| `/message/markAsRead` | POST | 标记消息为已读 |
| `/message/getUnreadCount` | GET | 获取未读消息数量 |
| `/message/sendReminder` | GET | 发送催还提醒 |

### 图片上传接口

| 接口路径 | HTTP方法 | 说明 |
|---------|---------|------|
| `/update/updateImg` | POST | 上传图书封面图片 |


---

## 五、错误码说明

### 通用错误码

| 错误码 | 含义 |
|-------|------|
| `-1` | 未登录或会话已过期 |
| `-2` | 不能操作自己（删除/修改自己） |
| `-3` | 权限不足 |
| `-5` | 系统至少需要保留一个超级管理员 |
| `-6` | 不能修改自己的角色 |

### 借阅相关错误码

| 错误码 | 含义 |
|-------|------|
| `-1` | 图书已归还（无法续借） |
| `-2` | 借阅记录不存在 |
| `-3` | 图书已逾期（无法续借） |
| `-4` | 已达到最大续借次数 |

---

## 六、数据表关系图

```
user 1:n borrow n:1 book_info
     |                |
     |                |
     v                v
operation_log      book_type
     ^
     |
user 1:n message
```

---

## 七、文件结构

```
backend/
├── src/main/java/com/example/bms/
│   ├── web/              # 控制器层
│   ├── service/         # 服务层
│   ├── mapper/          # 数据访问层
│   ├── model/           # 数据模型
│   ├── config/          # 配置类
│   ├── exception/       # 异常类
│   ├── interceptor/     # 拦截器
│   └── utils/           # 工具类

frontend/
├── src/
│   ├── views/           # 页面组件
│   ├── components/      # 公共组件
│   ├── api/             # API接口封装
│   ├── store/           # 状态管理
│   ├── router/          # 路由配置
│   └── utils/           # 工具函数
```




