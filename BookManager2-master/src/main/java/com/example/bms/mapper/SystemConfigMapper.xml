<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.bms.mapper.SystemConfigMapper">

    <resultMap id="BaseResultMap" type="com.example.bms.model.SystemConfig">
        <id column="configId" property="configId"/>
        <result column="configKey" property="configKey"/>
        <result column="configValue" property="configValue"/>
        <result column="configDesc" property="configDesc"/>
    </resultMap>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT configId, configKey, configValue, configDesc FROM system_config ORDER BY configId
    </select>

    <update id="updateByKey" parameterType="com.example.bms.model.SystemConfig">
        UPDATE system_config SET configValue = #{configValue} WHERE configKey = #{configKey}
    </update>

    <insert id="insertConfig" parameterType="com.example.bms.model.SystemConfig">
        INSERT INTO system_config (configKey, configValue, configDesc) VALUES (#{configKey}, #{configValue}, #{configDesc})
    </insert>

    <select id="selectByKey" resultMap="BaseResultMap">
        SELECT configId, configKey, configValue, configDesc FROM system_config WHERE configKey = #{configKey}
    </select>
</mapper>
